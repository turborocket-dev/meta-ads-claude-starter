name: Security audit

on:
  push:
    branches: [main]
    paths:
      - 'mcp-server/pyproject.toml'
      - 'mcp-server/uv.lock'
      - '.github/workflows/audit.yml'
  pull_request:
    paths:
      - 'mcp-server/pyproject.toml'
      - 'mcp-server/uv.lock'
  schedule:
    - cron: '0 9 * * 1'

jobs:
  pip-audit:
    name: pip-audit on MCP deps
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4 — pinned SHA (supply chain)

      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install pip-audit
        run: uv tool install pip-audit

      - name: Sync MCP dependencies
        working-directory: mcp-server
        run: uv sync --frozen

      - name: Run pip-audit
        working-directory: mcp-server
        run: uv run pip-audit --strict
